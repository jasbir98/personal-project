import React, { useState, useEffect } from "react";
import { ReactDOM } from "react";
import { Switch, Button, Typography, Container, Grid, Card, Divider, CardActionArea, CardContent, CardMedia, Box} from "@mui/material";
// {selectedId === -1 ? <img src={require("../images/welcome.gif")} alt="photo" style={{maxHeight: "170px", position: "absolute", top: "25%"}}/> : null}
export {
    Container,
    Switch,
    Typography,
    Grid,
    Divider,
    Card,
    CardActionArea,
    CardContent,
    CardMedia,
    Box,
    Button
};